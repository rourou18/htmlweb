# 网页开发小组项目 - 与第三次小组项目的对比文档

## 项目背景
本项目在第三次小组项目的基础上，进一步升级了功能与交互体验。通过全面引入jQuery库，彻底重构了原有的原生JavaScript逻辑，并新增了多个动态交互功能，实现了更合理的文件结构和更佳的用户体验。

## 新增与升级功能概览

本项目严格遵循“仅使用jQuery代码（禁止原生JavaScript）”的要求，实现了不少于5个核心功能：

1. **统一导航与系统时间展示（jQuery实现）**
   - **第三次项目**：各个页面的导航大多独立编写，较难统一维护，且静态展示。
   - **本次升级**：通过统一引入 `nav.js`，动态向页面中注入共享的导航栏（`nav.js` 中使用 `$(document).ready` 与 `$("...").each` 进行DOM操作），并使用jQuery实现了实时的系统时间展示，高亮当前所在页面的导航项。

2. **倒计时功能（节日倒数）**
   - **第三次项目**：无动态倒计时功能。
   - **本次升级**：新增了到下一次传统节日（端午节）的倒计时。通过jQuery定时器，精准倒数剩余的天、小时和分钟，并在顶部工具栏实时显示。

3. **各类动态页面特效（加载动画、返回顶部与图片交互）**
   - **第三次项目**：网页元素主要为静态，缺乏过渡动画和流畅的用户体验。
   - **本次升级**：
     - 使用 `$('body').hide().fadeIn(800);` 实现了平滑的页面加载动画。
     - 结合 `$(window).scroll()` 与 `$('#back-to-top').fadeIn()`，实现了智能显隐的返回顶部按钮，点击后通过 `.animate({scrollTop: 0}, 600)` 实现平滑滚动。
     - 图片使用 `$('img').hover(...)` 等 jQuery 事件和 `.animate()` 实现了交互时的特效。

4. **AJAX 异步交互（随机获取诗词名言）**
   - **第三次项目**：网页内容完全在本地硬编码，缺乏动态数据获取的能力。
   - **本次升级**：在底部页脚使用 `$.ajax()` 异步请求外部 API（如 Hitokoto 一言接口），动态抓取诗词名言并以 jQuery `.hide().fadeIn()` 的方式优雅地展示在页面底部。整个过程无需刷新页面。

5. **表单校验与动态表格生成（增删改功能）**
   - **第三次项目**：若有表单，往往只有基础验证或无验证。
   - **本次升级**（集中于 `form.html`）：
     - 深度应用 jQuery (`$('#confirmBtn').click()`, `$('#class').val()` 等) 实现对字段非空、长度、金额范围及验证码的实时校验。
     - 验证成功后，利用 jQuery 的节点创建能力，如 `$('<tr>').append(...)`，生成并动态添加包含捐款记录的表格行。
     - 使用 jQuery 事件代理和闭包，赋予了表格中“删除”按钮直接删除该行（`.fadeOut().remove()`）的能力。

6. **随机验证码生成**
   - **本次升级**：在捐款反馈页面，生成随机字符并在 Canvas 上绘制（核心DOM操作与数值获取完全依赖jQuery实现），有效防止机器人恶意提交。

## 结构优化与界面美化
- **站点完整性**：所有独立的HTML页面（如 `craft.html`, `dance.html`, `intro.html` 等）均通过脚本注入或直接引用，统一关联了全局导航。形成了完整的站点结构，彻底消除了孤立页面。
- **界面美化**：整体采用了“宣纸白”和“传统红”等具有非遗传统文化特色的配色体系，配合统一的阴影、圆角与动画过渡，提升了界面的高级感。

## 总结
通过本次升级，整个项目完全摆脱了对原生 JS（如 `document.getElementById` 等）的依赖，全面应用 jQuery 简便而强大的选择器、事件绑定、动画特效和 AJAX 通信能力。功能更丰富的同时，代码更加简洁、高效、规范，是一次成功的技术与体验的升级。
